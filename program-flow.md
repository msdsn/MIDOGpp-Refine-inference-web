First analysisId and imageId generated in the AnalyzePage file.
On the frontend we are constantly listining for changes in the database.
When an analyse written to the database by the backend we immediately assign it to the currentAnalysis variable by checking updates using analysisId.
The process works similarly for the currentImage

On the backend

